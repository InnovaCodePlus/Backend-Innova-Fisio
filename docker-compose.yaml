version: '3'

services:
  database:
    container_name: innova-fisio-database
    image: postgres:13.5

    environment:
      POSTGRES_DB: innova_fisio
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password

    ports:
      - "5432:5432"

    volumes:
      - ./postgres:/var/lib/postgresql/data